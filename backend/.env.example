PORT=5000
HOST=0.0.0.0
MONGODB_URI=
JWT_SECRET=
SESSION_SECRET=
NODE_ENV=development
FRONTEND_URL=
# For cloud deployment, set to 'render', 'railway', 'heroku', etc.
CLOUD_PLATFORM=
# Set to 'false' if your cloud platform doesn't support HTTPS or you're testing
COOKIE_SECURE=true
# Optional: Set cookie domain for subdomain sharing (e.g., '.yourdomain.com')
# Leave empty for same-origin cookies
COOKIE_DOMAIN=
